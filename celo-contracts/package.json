{
  "name": "celo-contracts",
  "version": "1.0.0",
  "description": "PetChainRegistry Solidity contract for the Celo network",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "lint": "solhint 'contracts/**/*.sol'"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "solhint": "^5.0.0"
  }
}
